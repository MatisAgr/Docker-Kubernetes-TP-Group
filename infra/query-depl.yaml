apiVersion: apps/v1

kind: Deployment

metadata:
  name: ms-query-dpl

spec:
  replicas: 1

  selector:
    matchLabels:
      app: query

  template:

    metadata:
      labels:
        app: query

    spec:
      containers:
        - name: query-container
          image: ms-query
          ports:
            - containerPort: 4002
